export namespace mainApiNamespace {
   export type login = {
      login?: string;
      sublogin?: string;
      password?: string;
   };
   export type requestPayloadType = {
      [key: string]: any;
   };
}
